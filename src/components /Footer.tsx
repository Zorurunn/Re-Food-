"use client";
import { Button, Stack } from "@mui/material";
type CustomButtonProps = {
  title: string;
  width: string;
  textSize: string;
};

export const Footer = () => {
  return <Stack>footer</Stack>;
};
